import 'package:flutter/material.dart';

class ScheduledExpenseScreen extends StatelessWidget {
  const ScheduledExpenseScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("Scheduled Expense");
  }
}
